#include "parse.h"

int parseQuery(std::vector<int> query, std::string query_path){
  // Open query file -> ofstream
  // Skip line starting with ">" and read everything after that -> std::string
  // Loop through each letter and convert to int -> std::vector<int>
}
int parseDB(std::vector<int> db, std::vector<int> offsets, std::string db_path, int db_size){

}